[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-agent-runtime"
version = "0.1.0"
description = "A small standalone runtime for creating multiple Python agents that can communicate with each other."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]

[tool.setuptools.packages.find]
where = ["."]
